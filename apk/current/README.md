# FaceBatch 0.7.0 release record

This directory preserves the controlling release identity and reconstructed build report for FaceBatch 0.7.0.

The exact APK and source ZIP are deliberately represented by hashes rather than placeholder files because their bytes were not present in this workspace. Add authentic copies only after verifying them against `SHA256SUMS.txt`.

Expected files:

- `FaceBatch-0.7.0.apk`
- `FaceBatch-0.7.0-source.zip`
- original `FaceBatch-0.7.0-build-report.txt`

Do not substitute a rebuilt older release. Do not increment or rewrite the historical hashes.
